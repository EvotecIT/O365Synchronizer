# O365Synchronizer Website Content

This folder contains curated docs and examples for the Evotec project hub.

Keep examples tenant-neutral and use `-WhatIf` for contact synchronization or deletion patterns. Do not publish client IDs, tenant IDs, secrets, or real user addresses from raw examples.
